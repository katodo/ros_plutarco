<?xml version="1.0"?>

<launch>

    <!-- Load Differential controller parameters -->
    <rosparam command="load" file="$(find plutarco_control)/config/control.yaml" />

    <!-- Start controllers -->
    <node name="base_controller_spawner" pkg="controller_manager" type="spawner" 
            args="unav_joint_publisher unav_velocity_controller --shutdown-timeout 3"/>

    <!-- Start EKF localization -->
    <!-- <node pkg="robot_localization" type="ekf_localization_node" name="ekf_localization"> -->
    <!--   <rosparam command="load" file="$(find plutarco_control)/config/localization.yaml" /> -->
    <!-- </node> -->

    <!-- Start Ultrasonic Bumper -->
    <!-- NOT IN PLUTARCO <include file="$(find plutarco_control)/launch/include/ultrasonic_bumper.launch" />	 -->	

    <!-- Start Ultrasonic Bumper Speed Filter -->
    <!-- NOT IN PLUTARCO <include file="$(find plutarco_control)/launch/include/ultrasonic_bumper_speed_filter.launch" /> -->    

    <!-- Interactive maker -->
    <!-- NOT IN PLUTARCO <node pkg="interactive_marker_twist_server" type="marker_server" name="twist_marker_server" output="screen"/>	 -->

</launch>
